{
  "name": "cc-validator-api",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "start": "ts-node ./src/index.ts",
    "dev": "nodemon ./src/index.ts",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "clean": "rm -rf ./dist",
    "build": "esbuild ./src/index.ts --bundle --platform=node --outdir=dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/supertest": "^2.0.12",
    "esbuild": "^0.18.14",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3",
    "tsconfig": "^0.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "luhn-checksum": "^1.0.0"
  }
}
